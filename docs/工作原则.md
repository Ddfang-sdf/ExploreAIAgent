现在先写测试代码，验收标准：全部代码100%按照文档实现，能够编译通过，所有测试均有合理的断言，测试必须调用设计文档中定义的公开方法入口。不得跳过上层方法直接测试内部私有方法。注意：你应该只写测试逻辑，严禁实现业务代码。为了测试代码能够编译通过，你可以先将业务代码实现成空壳代码。先于测试实现业务代码逻辑的行为，是极度不负责的。






确认测试代码是否 100% 按照设计文档实现。逐用例执行以下检查：
 1. 测试调用的入口方法与设计文档定义的方法是否一致（非上层代理）
 2. 测试传入的参数值经过该方法的处理后，按设计文档的逻辑，能否产出测试断言的预期值，测试必须调用设计文档中定义的公开方法入口。不得跳过上层方法直接测试内部私有方法
 3. 任何断言字段如果不由被调方法直接产出，标记为不一致，对每个断言，列出"输入值 → 设计逻辑 → 实际输出"的推导链，不可跳步
 禁止仅凭断言文字与设计文档"看起来相似"即判定一致。





现在开始实现业务代码，验收标准：全部代码100%按照文档实现，实现后必须运行测试，所有测试用例执行通过。注意：你应该只写业务逻辑，以文档设计为准，**严禁私自简化流程和代码**、**严禁私自设计降级逻辑**，测试用例执行不通过，应该首先检查代码实现是否有问题。如果评估发现测试用例代码实现不合理，立刻停止所有工作，向我汇报，由我决策